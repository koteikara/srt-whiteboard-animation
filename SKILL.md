---
name: srt-whiteboard-animation
description: SRT字幕から、暖かなベージュの紙を背景にした白板手描きアニメーションを生成する。字幕の解析、絵コンテ案の提示、統一した線画の生成、物語上の意味に基づく領域注釈、プレビューでの調整、MP4レンダリングを扱う。「SRTから白板アニメを作る」「字幕に合わせて手描き動画を作る」「字幕を絵コンテ化する」といった依頼で使用する。
---

# SRT白板アニメーション

SRT字幕を、物語の順序に沿って描かれる白板アニメーションへ変換する。領域マスクで未描画部分を隠し、各領域では筆先を骨格またはグリッドに沿って連続移動させる。`ink`で線を描いてから`color`で彩色し、描画済みの内容は共通キャンバス上に残す。ユーザー向けの説明、絵コンテ、設定名、画面表示には日本語を使う。

## 標準設定

| 項目 | 設定 |
|---|---|
| 背景 | 暖かな古紙色。生成時は`#F5EBD7`を推奨し、レンダリング時は画像の四隅付近から色を採る。純白にしない。 |
| 描画 | 領域ごとに`ink`から`color`へ進め、時間配分を2:1にする。 |
| 筆跡 | `--ink-path grid`を既定とし、線画が明瞭なら`skeleton`を使う。 |
| 彩色 | `--color-fill contour-wipe`を既定とし、必要なら`brush`を使う。 |
| 未描画領域 | `region`から後続領域と`protectedRegions`を除いた許可マスク内だけを描く。 |
| 幕の長さ | 字幕範囲から`sceneDurationMs`を決める。目安は25〜35秒。 |
| 編集枠 | プレビューでは番号付き編集枠を表示する。完成映像には含めない。 |

## 画像の統一規格

すべての幕で次の規格を守り、画像生成プロンプトにも明記する。

- 極簡素な手描き線画とし、Notion風の抑制された落書き表現、広い余白、簡潔な構図を使う。
- 背景を`#F5EBD7`、線を濃い灰色にする。赤、橙、青だけを控えめな差し色に使う。
- 人物や物体は輪郭と少数の線で表し、写実性より関係、変化、中心概念を優先する。
- 文字、単語、英数字、ラベル、写真表現、3D、絵画的質感、複雑な背景、高彩度色を入れない。
- ユーザーが筆素材上のロゴを明示的に残すよう求めた場合だけ、`drawing-hand.png`の筆記具上の表示を残す。

## 確認ゲート

各工程を終えるたびに停止し、ユーザーの明示的な確認を得てから次へ進む。無回答、包括的な事前承認、異議がないことを承認とみなさない。修正を求められたら該当工程だけをやり直し、再び確認を待つ。

例外として、注釈JSONを作成した直後はプレビューを自動で開き、そのJSONがあるフォルダーを読み込む。これは注釈工程の一部であり、別の確認を求めない。File System Access APIがユーザー操作を要求する場合は、ブラウザー上で確定済みのフォルダーを選ぶ。ユーザー自身に起動を委ねない。

## ワークフロー

1. `scripts/parse_srt.py`で字幕を解析し、25〜35秒を目安に幕を分ける。各幕について、番号、中心メッセージ、画面主体、字幕範囲、`sceneDurationMs`を示す。画像はまだ生成しない。提示後に停止し、確認を待つ。
2. 承認された案だけを基に、16:9の線画を幕ごとに生成する。画像の統一規格を守り、主体間に自動分割用の余白を設ける。提示後に停止し、確認を待つ。
3. 承認後、対応字幕を読んでから実画像を確認し、原画像の幅と高さを取得する。字幕の出来事を抽出し、見える主体を「場面設定 → 主要人物・物体 → 動作・変化 → 反応・結果」の順に対応付け、`<画像名>.annotation.json`を作る。直後に`assets/preview.html`を既定ブラウザーで開き、注釈ファイルのあるフォルダーから同名PNGとJSONを読み込む。読み込み後に停止し、確認を待つ。
4. 承認後、`render_annotation_preview.py`で番号と方向の確認画像を作る。領域がキャンバス内にあり、物語の順序と一致し、重なりが`protectedRegions`で保護されていることを確認する。停止して確認を待つ。
5. 承認後、プレビューで領域、名称、方向、開始・終了時刻、字幕、順序を調整する。保存時に`sequence`を連番にし、各領域の`subtitle`を保持し、`sceneDurationMs`を最後の終了時刻に0.5秒を加えた値へ合わせる。保存後に停止し、確認を待つ。
6. 承認後、`render_stream_whiteboard.py`で各幕をMP4へレンダリングする。冒頭、重複領域の途中、末尾を確認する。停止して完成動画の確認を待つ。
7. 複数幕の場合だけ、各幕の承認後に`merge_scenes.py`で順番どおりに結合する。停止して最終確認を待つ。

## ファイル配置

```text
assets/whiteboard/<プロジェクト名>/
  scene-01-<名前>.png
  scene-01-<名前>.annotation.json
  scene-01-<名前>-whiteboard.mp4
  scene-01-<名前>-preview.mp4
```

画像と注釈を同じ基底名にする。`foo.png`には`foo.annotation.json`を対応させる。

## 注釈規則

- 字幕と実画像の両方を確認してから注釈する。どちらかがなければ取得するまで作成しない。
- `sequence`、`startMs`、`label`を字幕の出来事の順に合わせる。画面上の位置だけで並べない。
- `canvas.width`と`canvas.height`を原画像の実寸に一致させる。`region`と`protectedRegions`には左上原点の整数ピクセル座標を使い、比率や推定値を使わない。
- 各要素に`sequence`、`narrativeRole`、`subtitle`、`region`、`reveal`、`handPath`を持たせる。`sequence`は1から連続させ、`narrativeRole`は日本語で記述する。
- すべての領域がキャンバス内にあり、対象を覆い、字幕の出来事と一致することを検証する。

## 時間とマスクの規則

- `sceneDurationMs`にはその幕の字幕範囲を使う。
- 一筆で描く表現を守るため、同じ幕の領域を原則として直列に配置する。必要なら領域間に100〜300ミリ秒の間を置く。
- 領域内の時間を`ink:color = 2:1`に分ける。初期値には字幕時間または「描画距離÷150ピクセル/秒」を使える。
- 全領域の完了後、完成画像を0.5秒以上保持する。
- 時刻`t`で表示できるのは、`reveal.startMs ≤ t`を満たし、その時点の描画進捗内にある画素だけとする。
- 許可マスクを「現在の`region` − 後続の全`region` − 現在の`protectedRegions`」として扱う。
- `direction`と`handPath`はプレビューの矩形代理にだけ使う。完成映像の筆跡は自動生成する。

## コマンド

依存関係はSkill内の`.venv`へ隔離する。

```bash
python scripts/prepare_env.py --check
python scripts/prepare_env.py
python scripts/parse_srt.py <字幕.srt> --target-sec 30 --min-sec 25 --max-sec 35
python scripts/render_annotation_preview.py <画像> <注釈> <出力画像>
<ENV_PY> scripts/render_stream_whiteboard.py <画像> <注釈> <出力.mp4> assets/drawing-hand.png \
  [--ink-path grid|skeleton] [--color-fill contour-wipe|brush] [--total-ms <ミリ秒>]
<ENV_PY> scripts/merge_scenes.py --inputs 幕1.mp4 幕2.mp4 幕3.mp4 --output final.mp4
```

`prepare_env.py --check`の最終行に出る`ENV_PY=<パス>`を後続処理で使う。プレビューはChromeまたはEdgeで`assets/preview.html`を開き、「フォルダーを開く」から対象フォルダーを読み込む。

## 品質確認

- 最初のフレームに未描画の線が出ていない。
- 原画像寸法と`canvas`が一致し、全領域が整数座標でキャンバス内に収まる。
- `sequence`と`startMs`が字幕の出来事の順序に一致する。
- 重複領域の途中で後続要素や保護領域が先に現れない。
- 筆先が進行中の線に近い。明瞭な線画では`skeleton`も試す。
- 完成画像を0.5秒以上保持する。
- 結合後の幕順と時間が絵コンテに一致する。

効果を直す場合は、先にプレビューで注釈を編集して保存し、その後で再レンダリングする。
